{
  "name": "compile-fixture-react",
  "version": "0.0.0",
  "private": true,
  "description": "Host project for scripts/verify-export-compile.mjs: a React export is pasted into somebody's project, so the harness supplies one — Tailwind, Motion, and next/image for the image component the export can emit.",
  "dependencies": {
    "motion": "^13.4.0",
    "next": "^15.5.4",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "typescript": "~5.6.3"
  }
}
