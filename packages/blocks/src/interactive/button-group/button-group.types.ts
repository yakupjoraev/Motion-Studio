import type { ButtonGroupProps as ButtonGroupSchemaProps } from './button-group.schema'

/** No slot: the items are the content, and a choice is a label rather than a subtree. */
export type ButtonGroupProps = ButtonGroupSchemaProps
