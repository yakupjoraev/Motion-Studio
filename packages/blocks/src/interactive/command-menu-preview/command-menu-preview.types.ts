import type { CommandMenuPreviewProps as CommandMenuPreviewSchemaProps } from './command-menu-preview.schema'

/** No slot: it is a picture, and a picture with a drop zone inside it would invite a real control into a fake one. */
export type CommandMenuPreviewProps = CommandMenuPreviewSchemaProps
