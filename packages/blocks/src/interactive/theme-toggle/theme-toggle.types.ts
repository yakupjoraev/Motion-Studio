import type { ThemeToggleProps as ThemeToggleSchemaProps } from './theme-toggle.schema'

/** No slot: a mode switch is three choices, and a choice is a glyph and a word. */
export type ThemeToggleProps = ThemeToggleSchemaProps
