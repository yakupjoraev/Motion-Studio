import type { ButtonProps as ButtonSchemaProps } from './button.schema'

/** No slot: a button is a label, and a button holding blocks is a card with a click handler. */
export type ButtonProps = ButtonSchemaProps
