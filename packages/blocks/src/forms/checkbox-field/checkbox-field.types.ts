import type { CheckboxFieldProps as CheckboxFieldSchemaProps } from './checkbox-field.schema'

/** No slot: the choices are a list prop, because a choice is a value and a word rather than a block. */
export type CheckboxFieldProps = CheckboxFieldSchemaProps
