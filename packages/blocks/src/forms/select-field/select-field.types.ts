import type { SelectFieldProps as SelectFieldSchemaProps } from './select-field.schema'

/** No slot: the choices are a list prop, because an option is a value and a word rather than a block. */
export type SelectFieldProps = SelectFieldSchemaProps
