import type { ChartPreviewProps as ChartPreviewSchemaProps } from './chart-preview.schema'

/** No slot: the data is a numeric array and everything else about the chart is a label. */
export type ChartPreviewProps = ChartPreviewSchemaProps
