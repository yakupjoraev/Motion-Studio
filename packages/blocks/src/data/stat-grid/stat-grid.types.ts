import type { StatGridProps as StatGridSchemaProps } from './stat-grid.schema'

/** No slot: the figures are a list prop, because a grid of statistics is data rather than a container. */
export type StatGridProps = StatGridSchemaProps
