import type { ProgressRingProps as ProgressRingSchemaProps } from './progress-ring.schema'

/** No slot: a meter is one value in a range, and everything else about it is a label. */
export type ProgressRingProps = ProgressRingSchemaProps
