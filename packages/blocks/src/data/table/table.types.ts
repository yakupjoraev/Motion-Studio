import type { TableProps as TableSchemaProps } from './table.schema'

/** No slot: a table is columns and rows, both edited as lists, and its cells are strings. */
export type TableProps = TableSchemaProps
