import type { FeatureSplitProps as FeatureSplitSchemaProps } from './feature-split.schema'

/** No slot: a row is copy and a picture, and both are props the inspector edits as a list. */
export type FeatureSplitProps = FeatureSplitSchemaProps
