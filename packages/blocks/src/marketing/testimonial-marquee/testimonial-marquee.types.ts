import type { TestimonialMarqueeProps as TestimonialMarqueeSchemaProps } from './testimonial-marquee.schema'

/** No slot: the cards are content, and the rows are dealt from one list. */
export type TestimonialMarqueeProps = TestimonialMarqueeSchemaProps
