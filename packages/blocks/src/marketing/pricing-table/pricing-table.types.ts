import type { PricingTableProps as PricingTableSchemaProps } from './pricing-table.schema'

/** No slot: a plan is a shape the inspector edits as a sortable list, not a place to drop blocks. */
export type PricingTableProps = PricingTableSchemaProps
