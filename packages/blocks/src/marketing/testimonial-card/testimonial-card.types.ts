import type { TestimonialCardProps as TestimonialCardSchemaProps } from './testimonial-card.schema'

/** No slot: a testimonial is words and a face, and both are props. */
export type TestimonialCardProps = TestimonialCardSchemaProps
