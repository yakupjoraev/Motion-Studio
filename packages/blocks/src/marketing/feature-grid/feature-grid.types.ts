import type { FeatureGridProps as FeatureGridSchemaProps } from './feature-grid.schema'

/** No slot: the cells are content, edited as a list, so the tree stays as shallow as the design is. */
export type FeatureGridProps = FeatureGridSchemaProps
