import type { HeroTerminalProps as HeroTerminalSchemaProps } from './hero-terminal.schema'

/** No slot: the window's content is a list of lines, which is a prop the inspector can edit. */
export type HeroTerminalProps = HeroTerminalSchemaProps
