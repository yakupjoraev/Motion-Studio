import type { HeroAppPreviewProps as HeroAppPreviewSchemaProps } from './hero-app-preview.schema'

/** No slot: the plate holds one image, and an image is a prop the inspector already knows how to edit. */
export type HeroAppPreviewProps = HeroAppPreviewSchemaProps
