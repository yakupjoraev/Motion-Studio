import type { HeroVideoProps as HeroVideoSchemaProps } from './hero-video.schema'

/** No slot: the backdrop is the block, and the copy sits directly on it. */
export type HeroVideoProps = HeroVideoSchemaProps
