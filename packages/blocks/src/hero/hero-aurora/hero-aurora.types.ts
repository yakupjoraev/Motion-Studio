import type { HeroAuroraProps as HeroAuroraSchemaProps } from './hero-aurora.schema'

/** No slot: the backdrop is the block, and nothing may be placed between it and the copy. */
export type HeroAuroraProps = HeroAuroraSchemaProps
