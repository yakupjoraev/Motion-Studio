import type { NavbarFloatingProps as NavbarFloatingSchemaProps } from './navbar-floating.schema'

/** No slot: a pill holds links and one button, and both are lists rather than subtrees. */
export type NavbarFloatingProps = NavbarFloatingSchemaProps
