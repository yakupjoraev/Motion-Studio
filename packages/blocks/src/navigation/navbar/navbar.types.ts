import type { NavbarProps as NavbarSchemaProps } from './navbar.schema'

/** No slot: the links are a sortable list, and a dropdown child is a label rather than a subtree. */
export type NavbarProps = NavbarSchemaProps
