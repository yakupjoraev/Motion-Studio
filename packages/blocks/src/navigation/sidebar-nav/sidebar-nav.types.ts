import type { SidebarNavProps as SidebarNavSchemaProps } from './sidebar-nav.schema'

/** No slot: a group is a sortable list of links, not a subtree the editor should own. */
export type SidebarNavProps = SidebarNavSchemaProps
