import type { MotionChannel, MotionSpec } from '@motion-studio/schema'

/** A quotation arrives with the band it interrupts. */
export const quoteMotion: Readonly<Partial<Record<MotionChannel, MotionSpec>>> = {}
