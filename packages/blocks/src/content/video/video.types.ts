import type { VideoProps as VideoSchemaProps } from './video.schema'

/** No slot: the caption is a prop, so the figure and its caption stay one node in the tree. */
export type VideoProps = VideoSchemaProps
