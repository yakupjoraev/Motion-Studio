import type { MotionChannel, MotionSpec } from '@motion-studio/schema'

/** Prose arrives with the band it sits in. */
export const richTextMotion: Readonly<Partial<Record<MotionChannel, MotionSpec>>> = {}
