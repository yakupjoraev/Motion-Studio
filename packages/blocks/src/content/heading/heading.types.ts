import type { HeadingProps as HeadingSchemaProps } from './heading.schema'

/** No slot: a heading is text. Children would be a second source of the same content. */
export type HeadingProps = HeadingSchemaProps
