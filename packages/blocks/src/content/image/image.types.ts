import type { ImageProps as ImageSchemaProps } from './image.schema'

/** No slot: the caption is a prop, so the figure and its caption stay one node in the tree. */
export type ImageProps = ImageSchemaProps
