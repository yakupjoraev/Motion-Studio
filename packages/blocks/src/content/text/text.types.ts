import type { TextProps as TextSchemaProps } from './text.schema'

/** No slot: a paragraph is text, and children would be a second source of the same content. */
export type TextProps = TextSchemaProps
