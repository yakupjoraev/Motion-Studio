# @motion-studio/theme

The runtime theme engine: palette generation, contrast repair, and the CSS variable set applied
to the document root.

Theming happens through CSS variables rather than class swapping, so a theme change never
re-renders the canvas.
