# @motion-studio/motion

The motion vocabulary: easing curves, spring configurations, the preset catalogue, the scheduler,
and the primitives blocks animate with.

Reduced motion is resolved here as a parallel design, not as a switch that removes animation at
the call site.
