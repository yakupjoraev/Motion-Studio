# @motion-studio/ui

The studio chrome primitives: buttons, panels, inputs, dialogs, popovers, tabs, sliders, and the
inspector controls — shadcn-derived, vendored, and adapted to our tokens.

These are the parts the user operates the editor with. They are never exported into generated
code; that is what `blocks` is for.
