# @motion-studio/dnd

The drag and drop layer over dnd-kit: sensors, drag previews, drop target resolution, and the
keyboard drag path with its live-region announcements.

Where a drop is legal is a pure function; where the pointer is, is not. The two are kept apart.
