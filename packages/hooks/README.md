# @motion-studio/hooks

React hooks shared by more than one package: keyboard and shortcut handling, media queries,
resize and intersection observation, and pointer gesture plumbing.

A hook that has exactly one caller stays colocated with that caller instead of landing here.
