import { nodeConfig } from '@motion-studio/config/vitest/node'

// No coverage floor: TESTING.md § Coverage contract lists ten packages and this is not one of
// them. The gap is escalated to the owner rather than filled with a number nobody can check.
export default nodeConfig
