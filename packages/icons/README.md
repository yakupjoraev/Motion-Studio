# @motion-studio/icons

The icon set as React components: one concept per file, sized by `currentColor` and the type
scale, no icon-font and no sprite sheet.

A leaf package. It knows nothing about the studio, the theme engine, or the registry.
