# @motion-studio/canvas

The infinite canvas as geometry: viewport transform, zoom and pan, hit testing, marquee, snapping,
alignment guides, rulers, and the overlay layer.

It does not know what it renders — it receives a render function. Everything hard here is a pure
function of numbers.
